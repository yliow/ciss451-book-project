\section{Inverse in modulo arithmetic}

In the key generation for RSA, Bob has to compute the
multiplicative inverse of $e$ mod $\phi(n)$. This is just the
Extended Euclidean Algorithm. (See previous notes).
